@model List<DTOLayer.TeamDtos.ResultTeamDto>
<section class="team container" id="ekibimiz">
    <div class="team-header">
        <span>👩‍⚕️ EKİBİMİZ</span>
        <h2>Uzman ve Deneyimli Kadromuz</h2>
    </div>

    <div class="team-grid">
        @foreach (var item in Model)
        {
            <div class="team-card">
                <div class="team-img">
                    <img src="https://i.imgur.com/5cX1Z4G.png" alt="">
                </div>
                <h3>@item.TeamName</h3>
                <span class="team-role">@item.TeamTitle</span>
                <p>
                    @item.TeamDescription
                </p>
            </div>
        }
    </div>
</section>